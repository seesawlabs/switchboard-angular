{
  "name": "inspinia",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "bower": "^1.3.12",
    "http-server": "^0.7.4"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "prestart": "npm install",
    "postinstall": "bower install",
    "start": "http-server ./ -a localhost -p 8001"
  }
}
